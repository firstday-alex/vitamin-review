{% if section.blocks.size != 0 %}

{%- style -%}
  #shopify-section-{{ section.id }} {
    --section-padding-top: {{ section.settings.padding_top }}px;
    --section-padding-bottom: {{ section.settings.padding_bottom }}px;
    --faq-primary: {{ section.settings.primary_color }};
    --faq-item-bg: {{ section.settings.item_bg_color }};
    --faq-item-border: {{ section.settings.item_border_color }};
    --faq-question-color: {{ section.settings.question_text_color }};
    --faq-answer-color: {{ section.settings.answer_text_color }};
    --faq-icon-bg: {{ section.settings.icon_bg_color }};
    --faq-icon-color: {{ section.settings.icon_color }};
    --faq-icon-bg-open: {{ section.settings.icon_bg_color_open }};
    --faq-icon-color-open: {{ section.settings.icon_color_open }};
    --faq-question-size: {{ section.settings.question_font_size }}px;
    --faq-answer-size: {{ section.settings.answer_font_size }}px;
    --faq-question-weight: {{ section.settings.question_weight }};
    --faq-item-radius: {{ section.settings.item_radius }}px;
    --faq-item-gap: {{ section.settings.item_gap }}px;
    --faq-border-width: {{ section.settings.border_width }}px;
    --faq-item-padding: {{ section.settings.item_padding }}px;
    --faq-max-width: {{ section.settings.max_width }}px;
    --faq-icon-size: {{ section.settings.icon_size }}px;
  }
{%- endstyle -%}

{{ 'section-faq.css' | asset_url | stylesheet_tag }}

<div
  class="section--padding{% if section.settings.show_divider %} section--divider{% endif %}"
  data-faq-icon="{{ section.settings.icon_style }}"
  {% if section.settings.single_open %}data-faq-single-open{% endif %}
  data-faq-item-style="{{ section.settings.item_style }}"
>
  {%- if section.settings.secondary_background -%}
    <div class="faq--colored">
  {%- endif -%}
    <div class="faq faq--upgraded faq--{{ section.settings.item_style }} page-width{% if section.settings.narrow %} page-width--inner{% endif %}">
      <div class="title-wrapper{% if section.settings.heading == blank %} title-wrapper--no-heading{% endif %} {{ section.settings.heading_alignment }}">
        <{{ section.settings.heading_tag }} class="title {{ section.settings.heading_size }}">{{ section.settings.heading | escape }}</{{ section.settings.heading_tag }}>
      </div>

      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'rich-text' -%}
            <p class="h3" {{ block.shopify_attributes }}>{{ block.settings.title | escape }}</p>
            <div class="rte">{{ block.settings.text }}</div>
          {%- when 'question' -%}
            <accordion-tab {{ block.shopify_attributes }}>
              <details>
                <summary>
                  <span>
                    {%- case section.settings.icon_style -%}
                      {%- when 'chevron' -%}
                        <span class="faqicon faqicon--chevron" aria-hidden="true">
                          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M5 8l5 5 5-5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                        </span>
                      {%- when 'caret' -%}
                        <span class="faqicon faqicon--caret" aria-hidden="true">
                          <svg width="14" height="14" viewBox="0 0 20 20" fill="currentColor"><path d="M5 7l5 6 5-6z"/></svg>
                        </span>
                      {%- when 'arrow' -%}
                        <span class="faqicon faqicon--arrow" aria-hidden="true">
                          <svg width="18" height="18" viewBox="0 0 20 20" fill="none"><path d="M5 10h10M11 6l4 4-4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                        </span>
                      {%- when 'none' -%}
                        {%- comment -%} no icon {%- endcomment -%}
                      {%- else -%}
                        <span class="faqicon icon-plus-alt"></span>
                    {%- endcase -%}
                    <span class="faq-question-text">{{ block.settings.title | escape }}</span>
                  </span>
                </summary>
                <div class="accordion__content rte typeset">
                  {{ block.settings.text }}
                </div>
              </details>
            </accordion-tab>
        {%- endcase -%}
      {%- endfor -%}
    </div>
  {%- if section.settings.secondary_background -%}
    </div>
  {%- endif -%}
</div>

{%- endif -%}

{% stylesheet %}
/* ─── Upgraded FAQ UI ───
   Enhances the existing .faq structure with configurable look-and-feel.
   All colors, sizes, and shapes drive from CSS variables declared in the
   per-instance <style> block at the top of the section. */

.faq--upgraded {
  max-width: var(--faq-max-width, 1100px);
  margin-inline: auto;
}

.faq--upgraded accordion-tab {
  display: block;
  margin-bottom: var(--faq-item-gap, 12px);
  transition: border-color 0.2s ease, box-shadow 0.2s ease, background 0.2s ease;
}

/* ── Card style (bordered, rounded) ── */
.faq--card accordion-tab {
  background: var(--faq-item-bg, #FFFFFF);
  border: var(--faq-border-width, 1px) solid var(--faq-item-border, #CCCCCC);
  border-radius: var(--faq-item-radius, 16px);
  overflow: hidden;
}
.faq--card accordion-tab:hover {
  border-color: color-mix(in srgb, var(--faq-primary, #486CED) 50%, var(--faq-item-border, #CCCCCC));
}
.faq--card accordion-tab:has(details[open]) {
  border-color: var(--faq-primary, #486CED);
  box-shadow: 0 6px 20px color-mix(in srgb, var(--faq-primary, #486CED) 12%, transparent);
}

/* ── Divider style (hairline separators, no card chrome) ── */
.faq--divider accordion-tab {
  border-top: var(--faq-border-width, 1px) solid var(--faq-item-border, #CCCCCC);
  border-radius: 0;
  background: transparent;
  margin-bottom: 0;
}
.faq--divider accordion-tab:last-of-type {
  border-bottom: var(--faq-border-width, 1px) solid var(--faq-item-border, #CCCCCC);
}

/* ── Minimal style (no borders, just padding/spacing) ── */
.faq--minimal accordion-tab {
  background: transparent;
  border: 0;
  border-radius: 0;
}

/* ── Summary / question row ── */
.faq accordion-tab details summary {
  padding: var(--faq-item-padding, 18px);
  cursor: pointer;
  font-size: var(--faq-question-size, 18px);
  font-weight: var(--faq-question-weight, 700);
  color: var(--faq-question-color, #2C3E50);
  line-height: 1.4;
  transition: color 0.15s ease;
  list-style: none;
}
.faq--upgraded accordion-tab summary::-webkit-details-marker { display: none; }
.faq--upgraded accordion-tab summary::marker { display: none; content: ''; }
.faq--upgraded accordion-tab summary:focus-visible {
  outline: 2px solid var(--faq-primary, #486CED);
  outline-offset: 2px;
}
.faq--upgraded accordion-tab summary:hover {
  color: var(--faq-primary, #486CED);
}

.faq--upgraded accordion-tab summary > span {
  display: flex;
  align-items: center;
  gap: 14px;
  width: 100%;
}
.faq--upgraded .faq-question-text {
  flex: 1;
  min-width: 0;
}

/* ── Icon ── */
.faq--upgraded .faqicon {
  flex-shrink: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: var(--faq-icon-size, 32px);
  height: var(--faq-icon-size, 32px);
  border-radius: 50%;
  background: var(--faq-icon-bg, color-mix(in srgb, var(--faq-primary, #486CED) 10%, transparent));
  color: var(--faq-icon-color, var(--faq-primary, #486CED));
  transition: background 0.2s ease, color 0.2s ease, transform 0.25s ease;
  font-size: 14px;
}
.faq--upgraded accordion-tab details[open] .faqicon {
  background: var(--faq-icon-bg-open, var(--faq-primary, #486CED));
  color: var(--faq-icon-color-open, #fff);
}

/* Icon animations — sweep clockwise (to the right) on open */
.faq--upgraded .faqicon--chevron svg,
.faq--upgraded .faqicon--caret svg,
.faq--upgraded .faqicon--arrow svg {
  transition: transform 0.25s ease;
}
.faq--upgraded accordion-tab details[open] .faqicon--chevron svg,
.faq--upgraded accordion-tab details[open] .faqicon--caret svg {
  transform: rotate(180deg);
}
.faq--upgraded .icon-plus-alt {
  transition: transform 0.25s ease;
  display: inline-block;
}
.faq--upgraded accordion-tab details[open] .icon-plus-alt {
  transform: rotate(45deg);
}

/* Override the base theme's `.faqicon` wrapper rotation so only the SVG
   inside spins. Without this, the wrapper rotates 90° on hover and
   composes with the SVG's own rotation (visual rotation = 180° = LEFT).
   Scoped tightly to the same selector the theme uses, so the open-state
   plus rotation (on `.icon-plus-alt`) is untouched. */
@media screen and (hover: hover) {
  .faq--upgraded details:not([open]) summary:hover .faqicon {
    transform: none !important;
  }
}

/* Arrow: three-stage counter-clockwise rotation
   - closed:  rotate(90deg)  → points down ↓
   - hover:   rotate(0deg)   → points right →
   - open:    rotate(-90deg) → points up ↑ */
.faq--upgraded .faqicon--arrow svg {
  transform: rotate(90deg);
}
.faq--upgraded accordion-tab summary:hover .faqicon--arrow svg {
  transform: rotate(0deg);
}
.faq--upgraded accordion-tab details[open] .faqicon--arrow svg {
  transform: rotate(-90deg);
}

/* ── Answer content ── */
.faq--upgraded .accordion__content {
  padding: 4px var(--faq-item-padding, 18px) var(--faq-item-padding, 18px);
  font-size: var(--faq-answer-size, 15px);
  line-height: 1.65;
  color: var(--faq-answer-color, #2C3E50);
}
.faq--upgraded .accordion__content > *:first-child { margin-top: 0; }
.faq--upgraded .accordion__content > *:last-child { margin-bottom: 0; }
.faq--upgraded .accordion__content a {
  color: var(--faq-primary, #486CED);
  text-decoration: underline;
  text-underline-offset: 3px;
}
.faq--upgraded .accordion__content ul,
.faq--upgraded .accordion__content ol { padding-inline-start: 1.25rem; }
.faq--upgraded .accordion__content p { margin: 0 0 0.75em; }
.faq--upgraded .accordion__content p:last-child { margin-bottom: 0; }

/* ── Rich-text block (between questions) ── */
.faq--upgraded > p.h3 {
  margin: 1.5rem 0 0.5rem;
  color: var(--faq-question-color, #2C3E50);
}

/* ── Mobile ── */
@media screen and (max-width: 749px) {
  .faq--upgraded accordion-tab summary {
    padding: 14px;
    font-size: calc(var(--faq-question-size, 18px) - 2px);
  }
  .faq--upgraded .accordion__content {
    padding: 2px 14px 14px;
  }
  .faq--upgraded .faqicon {
    width: calc(var(--faq-icon-size, 32px) - 4px);
    height: calc(var(--faq-icon-size, 32px) - 4px);
  }
}

@media (prefers-reduced-motion: reduce) {
  .faq--upgraded *, .faq--upgraded *::before, .faq--upgraded *::after {
    transition-duration: 0.01ms !important;
  }
}
{% endstylesheet %}

{% javascript %}
/* Single-open behavior: when a <details> inside a [data-faq-single-open]
   container opens, close its siblings. Uses the native 'toggle' event so
   it works with the existing <accordion-tab> wrapper without touching its
   internals. */
document.addEventListener('toggle', (e) => {
  const details = e.target;
  if (!(details instanceof HTMLDetailsElement)) return;
  if (!details.open) return;
  const container = details.closest('[data-faq-single-open]');
  if (!container) return;
  container.querySelectorAll('details[open]').forEach((d) => {
    if (d !== details) d.open = false;
  });
}, true);
{% endjavascript %}

{% schema %}
{
  "name": "t:sections.faq.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "narrow",
      "default": false,
      "label": "t:sections.all.narrow.label"
    },
    {
      "type": "checkbox",
      "id": "secondary_background",
      "default": false,
      "label": "t:sections.faq.settings.secondary_background.label"
    },
    {
      "type": "checkbox",
      "id": "show_divider",
      "default": false,
      "label": "t:sections.all.show_divider.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.heading.label"
    },
    {
      "type": "text",
      "id": "heading",
      "default": "FAQs",
      "label": "t:sections.all.heading.label"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__1.label"
        },
        {
          "value": "h1",
          "label": "t:sections.all.heading_size.options__2.label"
        },
        {
          "value": "h0",
          "label": "t:sections.all.heading_size.options__3.label"
        }
      ],
      "default": "h1",
      "label": "t:sections.all.heading_size.label"
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "t:sections.all.heading_alignment.label",
      "options": [
        {
          "value": "left",
          "label": "t:sections.all.heading_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.all.heading_alignment.options__2.label"
        },
        {
          "value": "right",
          "label": "t:sections.all.heading_alignment.options__3.label"
        }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "options": [
        {
          "value": "h1",
          "label": "t:sections.all.heading_tag.options__1.label"
        },
        {
          "value": "h2",
          "label": "t:sections.all.heading_tag.options__2.label"
        },
        {
          "value": "h3",
          "label": "t:sections.all.heading_tag.options__3.label"
        },
        {
          "value": "h4",
          "label": "t:sections.all.heading_tag.options__4.label"
        },
        {
          "value": "h5",
          "label": "t:sections.all.heading_tag.options__5.label"
        },
        {
          "value": "h6",
          "label": "t:sections.all.heading_tag.options__6.label"
        },
        {
          "value": "div",
          "label": "t:sections.all.heading_tag.options__7.label"
        },
        {
          "value": "span",
          "label": "t:sections.all.heading_tag.options__8.label"
        },
        {
          "value": "p",
          "label": "t:sections.all.heading_tag.options__9.label"
        }
      ],
      "default": "h2",
      "label": "t:sections.all.heading_tag.label",
      "info": "t:sections.all.heading_tag.info"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.header.content"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "t:sections.all.padding.padding_top.unit",
      "label": "t:sections.all.padding.padding_top.label",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "t:sections.all.padding.padding_bottom.unit",
      "label": "t:sections.all.padding.padding_bottom.label",
      "default": 36
    },
    {
      "type": "header",
      "content": "FAQ item — Style"
    },
    {
      "type": "select",
      "id": "item_style",
      "label": "Item style",
      "options": [
        { "value": "card", "label": "Card (bordered, rounded)" },
        { "value": "divider", "label": "Divider (hairline rules)" },
        { "value": "minimal", "label": "Minimal (no chrome)" }
      ],
      "default": "card"
    },
    {
      "type": "select",
      "id": "icon_style",
      "label": "Icon style",
      "options": [
        { "value": "plus", "label": "Plus (+)" },
        { "value": "chevron", "label": "Chevron (v)" },
        { "value": "caret", "label": "Caret (▾)" },
        { "value": "arrow", "label": "Arrow (→)" },
        { "value": "none", "label": "No icon" }
      ],
      "default": "chevron"
    },
    {
      "type": "checkbox",
      "id": "single_open",
      "label": "Only one question open at a time",
      "default": false
    },
    {
      "type": "range",
      "id": "max_width",
      "label": "Max FAQ width (px)",
      "min": 600,
      "max": 1600,
      "step": 20,
      "default": 1100
    },
    {
      "type": "range",
      "id": "item_gap",
      "label": "Gap between items (px)",
      "min": 0,
      "max": 32,
      "step": 1,
      "default": 10
    },
    {
      "type": "range",
      "id": "item_padding",
      "label": "Item inner padding (px)",
      "min": 8,
      "max": 40,
      "step": 1,
      "default": 18
    },
    {
      "type": "range",
      "id": "item_radius",
      "label": "Item corner radius (card style, px)",
      "min": 0,
      "max": 32,
      "step": 1,
      "default": 16
    },
    {
      "type": "range",
      "id": "border_width",
      "label": "Border / divider width (px)",
      "min": 0,
      "max": 4,
      "step": 1,
      "default": 1
    },
    {
      "type": "range",
      "id": "icon_size",
      "label": "Icon size (px)",
      "min": 20,
      "max": 56,
      "step": 1,
      "default": 32
    },
    {
      "type": "header",
      "content": "FAQ item — Colors"
    },
    {
      "type": "color",
      "id": "primary_color",
      "label": "Primary (open state, hover, links)",
      "default": "#486CED"
    },
    {
      "type": "color",
      "id": "item_bg_color",
      "label": "Item background (card style)",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "item_border_color",
      "label": "Item border / divider",
      "default": "#CCCCCC"
    },
    {
      "type": "color",
      "id": "question_text_color",
      "label": "Question text",
      "default": "#2C3E50"
    },
    {
      "type": "color",
      "id": "answer_text_color",
      "label": "Answer text",
      "default": "#2C3E50"
    },
    {
      "type": "color",
      "id": "icon_bg_color",
      "label": "Icon background (closed state)",
      "default": "#EEF2FF"
    },
    {
      "type": "color",
      "id": "icon_color",
      "label": "Icon color (closed state)",
      "default": "#486CED"
    },
    {
      "type": "color",
      "id": "icon_bg_color_open",
      "label": "Icon background (open state)",
      "default": "#486CED"
    },
    {
      "type": "color",
      "id": "icon_color_open",
      "label": "Icon color (open state)",
      "default": "#FFFFFF"
    },
    {
      "type": "header",
      "content": "FAQ item — Typography"
    },
    {
      "type": "range",
      "id": "question_font_size",
      "label": "Question font size (px)",
      "min": 12,
      "max": 28,
      "step": 1,
      "default": 18
    },
    {
      "type": "range",
      "id": "question_weight",
      "label": "Question font weight",
      "min": 300,
      "max": 900,
      "step": 100,
      "default": 700
    },
    {
      "type": "range",
      "id": "answer_font_size",
      "label": "Answer font size (px)",
      "min": 12,
      "max": 22,
      "step": 1,
      "default": 15
    }
  ],
  "blocks": [
    {
      "type": "rich-text",
      "name": "t:sections.faq.blocks.rich-text.name",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "default": "Rich text",
          "label": "t:sections.faq.blocks.rich-text.settings.title.label"
        },
        {
          "type": "richtext",
          "id": "text",
          "default": "<p>Use this section for any descriptive text you need to fill out your pages or to add introductory headings between other blocks.</p>",
          "label": "t:sections.faq.blocks.rich-text.settings.text.label"
        }
      ]
    },
    {
      "type": "question",
      "name": "t:sections.faq.blocks.question.name",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "default": "Frequently asked question",
          "label": "t:sections.faq.blocks.question.settings.title.label"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "t:sections.faq.blocks.question.settings.text.label",
          "default": "<p>Use this text to answer questions in as much detail as possible for your customers.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.faq.presets.name",
      "blocks": [
        {
          "type": "question"
        },
        {
          "type": "question"
        }
      ]
    }
  ],
  "disabled_on": {
    "groups": ["header", "footer", "custom.overlay"]
  }
}
{% endschema %}
